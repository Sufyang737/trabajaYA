import OnboardingTransitionLayout from "@/components/onboarding/transition-layout";

export default function OnboardingGroupLayout({ children }: { children: React.ReactNode }) {
  return <OnboardingTransitionLayout>{children}</OnboardingTransitionLayout>;
}

